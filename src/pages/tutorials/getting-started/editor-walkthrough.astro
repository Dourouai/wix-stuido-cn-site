---
import ArticleLayout from '../../../layouts/ArticleLayout.astro';
import Icon from '../../../components/Icon.astro';

const toc = [
  { id: 'summary', title: '30秒核心速览' },
  { id: 'top-bar', title: '1. 顶部控制栏：断点标尺条 (Breakpoint Bar)' },
  { id: 'left-panel', title: '2. 左侧添加面板：核心元素结构' },
  { id: 'inspector', title: '3. 右侧属性检查器：控制流体尺寸的命脉' },
  { id: 'layers', title: '4. 图层面板：理解容器与 DOM 嵌套树' },
  { id: 'bottom-code', title: '5. 底部代码面板：Velo 开发者模式' },
];
---

<ArticleLayout
  title="深入浅出：Wix Studio 工作台与核心工具面板完全图解"
  description="完全解析 Wix Studio 编辑器界面四大核心区域：顶部断点标尺、左侧组件库、右侧属性检查器与图层嵌套树。掌握专业响应式排版的核心操作逻辑。"
  keywords="Wix Studio 界面, Wix Studio 工具面板, Wix 断点设置, Wix Studio 检查器, Wix 布局"
  category="新手入门实战"
  categorySlug="/tutorials/getting-started/"
  readTime="12 分钟"
  publishDate="2026-09-20"
  heroImage="/assets/images/tutorial-featured-cover.jpg"
  toc={toc}
  prevArticle={{
    title: "从零开始：Wix Studio 账号注册与团队工作空间初始化",
    href: "/tutorials/getting-started/account-setup/",
  }}
  nextArticle={{
    title: "域名绑定与解析完全指南：从阿里云/腾讯云到 Wix Studio",
    href: "/tutorials/getting-started/connect-domain/",
  }}
>
  <!-- 30s Executive Summary -->
  <section id="summary" class="bg-gradient-to-br from-purple-50/80 via-white to-blue-50/50 p-6 sm:p-8 rounded-2xl border border-purple-200/80 shadow-xs space-y-3">
    <div class="flex items-center gap-2 text-[#6D28D9] text-xs font-bold uppercase tracking-wider">
      <Icon name="sparkles" class="w-4 h-4" />
      <span>30秒核心速览 (Executive Summary)</span>
    </div>
    <p class="text-sm sm:text-base text-slate-800 leading-relaxed font-medium">
      第一次打开 Wix Studio 编辑器，很多习惯了普通拖拽建站的用户可能会被丰富的功能面板震撼。它看起来更像 Figma + VS Code 的结合体。
    </p>
    <p class="text-xs sm:text-sm text-slate-600 leading-relaxed">
      掌握它的核心只要记住一个原则：<strong>“结构看左侧图层树，行为看顶部断点尺，样式看右侧检查器”</strong>。只要搞懂右侧检查器中的“尺寸（Responsive Behavior）”逻辑，您就已经掌握了 Wix Studio 80% 的核心精髓。
    </p>
  </section>

  <!-- Section 1: Top Bar -->
  <section id="top-bar" class="space-y-4">
    <h2 class="text-xl sm:text-2xl font-bold text-slate-900 flex items-center gap-2">
      <span class="w-2 h-6 bg-[#6D28D9] rounded-full inline-block"></span>
      <span>1. 顶部控制栏：断点标尺条 (Breakpoint Bar)</span>
    </h2>
    <p>
      编辑器顶部最显眼的特征是横向贯穿的<strong>断点标尺（Breakpoint Bar）</strong>：
    </p>
    <div class="p-5 rounded-2xl bg-slate-50 border border-slate-200 space-y-3 text-xs sm:text-sm">
      <div class="font-bold text-slate-900 flex items-center gap-2">
        <Icon name="ruler" class="w-4 h-4 text-[#6D28D9]" />
        <span>默认三大基础断点划分：</span>
      </div>
      <ul class="space-y-2 pl-4 list-disc text-slate-600">
        <li><strong>桌面端（Desktop）：</strong> 默认基准断点为 `1001px 及以上`（通常以 1440px 为设计画板）。在此断点下的改动会自动<strong>级联继承（Cascade Down）</strong>到平板和移动端。</li>
        <li><strong>平板端（Tablet）：</strong> 覆盖 `768px - 1000px`。当您切换到此断点微调文字或间距时，只会覆盖平板及更小屏幕，不会破坏桌面端。</li>
        <li><strong>移动端（Mobile）：</strong> 覆盖 `320px - 767px`。通常用于将横向多列改为单列竖排、调整汉堡菜单与移动端触控按钮。</li>
      </ul>
      <p class="text-slate-500 pt-1 text-[11px]">
        *高级提示：点击标尺右侧的加号 `+`，您可以添加任意无限数量的自定义断点（如 1920px 超宽屏专享断点）。
      </p>
    </div>
  </section>

  <!-- Section 2: Left Panel -->
  <section id="left-panel" class="space-y-4">
    <h2 class="text-xl sm:text-2xl font-bold text-slate-900 flex items-center gap-2">
      <span class="w-2 h-6 bg-[#6D28D9] rounded-full inline-block"></span>
      <span>2. 左侧添加面板：核心元素结构</span>
    </h2>
    <p>
      点击左上角快捷图标即可展开添加面板。Studio 规范了严格的现代网页文档流层级：
    </p>
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 text-xs sm:text-sm">
      <div class="p-4 rounded-xl border border-slate-200 bg-white space-y-1.5">
        <div class="font-bold text-slate-900 flex items-center gap-1.5">
          <Icon name="layout" class="w-4 h-4 text-[#6D28D9]" />
          <span>Section（章节视口）</span>
        </div>
        <p class="text-slate-500 text-xs">
          页面的第一级横向承载块。通常设置全宽 100%，或固定视口高度（100vh）。
        </p>
      </div>

      <div class="p-4 rounded-xl border border-slate-200 bg-white space-y-1.5">
        <div class="font-bold text-slate-900 flex items-center gap-1.5">
          <Icon name="layers" class="w-4 h-4 text-blue-600" />
          <span>Container（内容容器）</span>
        </div>
        <p class="text-slate-500 text-xs">
          页面核心包装器。限制最大宽度（如 max-w-1280px），利用 CSS Grid 将子元素整齐组织。
        </p>
      </div>

      <div class="p-4 rounded-xl border border-slate-200 bg-white space-y-1.5">
        <div class="font-bold text-slate-900 flex items-center gap-1.5">
          <Icon name="palette" class="w-4 h-4 text-indigo-600" />
          <span>Quick Add（基础元组件）</span>
        </div>
        <p class="text-slate-500 text-xs">
          Text 标题段落、Image 高清媒体、Button 交互按钮、Video 视频播放框。
        </p>
      </div>

      <div class="p-4 rounded-xl border border-slate-200 bg-white space-y-1.5">
        <div class="font-bold text-slate-900 flex items-center gap-1.5">
          <Icon name="shopping-bag" class="w-4 h-4 text-emerald-600" />
          <span>App Widgets（商业业务组件）</span>
        </div>
        <p class="text-slate-500 text-xs">
          一键挂载 Wix Stores 商城模块、Wix Blog 博客文章列表、Wix Bookings 预约系统。
        </p>
      </div>
    </div>
  </section>

  <!-- Section 3: Inspector Panel -->
  <section id="inspector" class="space-y-4">
    <h2 class="text-xl sm:text-2xl font-bold text-slate-900 flex items-center gap-2">
      <span class="w-2 h-6 bg-[#6D28D9] rounded-full inline-block"></span>
      <span>3. 右侧属性检查器：控制流体尺寸的命脉</span>
    </h2>
    <p>
      选中国画布上的任意元素，右侧属性检查器（Inspector）将展示它的全部渲染参数。其中最核心的设置项是 <strong>Responsive Behavior（响应式行为）</strong>：
    </p>
    <div class="overflow-x-auto rounded-2xl border border-slate-200 shadow-xs">
      <table class="w-full text-left text-xs sm:text-sm">
        <thead class="bg-slate-50 border-b border-slate-200 text-slate-900 font-bold">
          <tr>
            <th class="p-3.5">响应式行为选项</th>
            <th class="p-3.5">底层计算机制</th>
            <th class="p-3.5">最适用元素</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-slate-100 text-slate-700">
          <tr class="hover:bg-slate-50/80">
            <td class="p-3.5 font-bold text-[#6D28D9]">Scale Proportionally (等比缩放)</td>
            <td class="p-3.5">元素宽高随屏幕视口等比例缩放（基于 vw 计算）</td>
            <td class="p-3.5 text-slate-500">大标题、卡片整体、带背景图的 Hero 区</td>
          </tr>
          <tr class="hover:bg-slate-50/80">
            <td class="p-3.5 font-bold text-slate-900">Fixed (固定像素)</td>
            <td class="p-3.5">无论屏幕如何变化，尺寸严格锁定 px 值不变</td>
            <td class="p-3.5 text-slate-500">Logo 图标、导航栏高度、装饰性小圆点</td>
          </tr>
          <tr class="hover:bg-slate-50/80">
            <td class="p-3.5 font-bold text-slate-900">Relative Width (相对宽度)</td>
            <td class="p-3.5">宽度根据父容器的百分比（%）自适应拉伸</td>
            <td class="p-3.5 text-slate-500">长文段落内容宽度、分割线、进度条</td>
          </tr>
          <tr class="hover:bg-slate-50/80">
            <td class="p-3.5 font-bold text-slate-900">Fit to Content (适应内容)</td>
            <td class="p-3.5">容器尺寸随内部文字长度或图片自动撑开</td>
            <td class="p-3.5 text-slate-500">徽章药丸胶囊、操作按钮、标签 Chip</td>
          </tr>
        </tbody>
      </table>
    </div>
  </section>

  <!-- Section 4: Layers Tree -->
  <section id="layers" class="space-y-4">
    <h2 class="text-xl sm:text-2xl font-bold text-slate-900 flex items-center gap-2">
      <span class="w-2 h-6 bg-[#6D28D9] rounded-full inline-block"></span>
      <span>4. 图层面板：理解容器与 DOM 嵌套树</span>
    </h2>
    <p>
      很多新手在做页面时，喜欢将文字和图片随手拖到空白画布上，结果在手机端看时全部错位乱飞。<strong>原因就是缺少了 Container（容器）包裹。</strong>
    </p>
    <div class="p-5 rounded-2xl bg-amber-50/70 border border-amber-200 text-xs sm:text-sm space-y-2 text-amber-950">
      <div class="font-bold flex items-center gap-1.5 text-amber-900">
        <Icon name="alert-triangle" class="w-4 h-4 text-amber-600" />
        <span>必须遵守的专业容器规范（Stack 堆栈）：</span>
      </div>
      <p>
        当您有“标题 + 副标题 + 按钮”一组元素时，选中这三者，点击右键选择 <strong>Stack（创建堆栈）</strong>。Stack 会自动以 Flex 竖向排列约束它们，并允许设置恒定的竖向间距（Gap）。这样无论在任何屏幕上，这三者都会作为一个整体协同自适应，永不重叠。
      </p>
    </div>
  </section>

  <!-- Section 5: Bottom Code -->
  <section id="bottom-code" class="space-y-4">
    <h2 class="text-xl sm:text-2xl font-bold text-slate-900 flex items-center gap-2">
      <span class="w-2 h-6 bg-[#6D28D9] rounded-full inline-block"></span>
      <span>5. 底部代码面板：Velo 开发者模式</span>
    </h2>
    <p>
      在编辑器左侧点击 `{ }` 图标开启 <strong>Dev Mode（开发者模式）</strong>，底栏将弹出内置的代码控制台：
    </p>
    <ul class="space-y-2 pl-4 text-xs sm:text-sm text-slate-700 list-disc">
      <li><strong>前端代码（Page Code）：</strong> 可以监听页面按钮的 `$w('#myButton').onClick(...)` 事件，实现自定义弹窗或动态动画。</li>
      <li><strong>全局样式（Global CSS）：</strong> 直接在此处添加自定义 CSS 规则与类名。</li>
      <li><strong>后端云函数（Backend）：</strong> 在 `backend/` 目录下编写 Node.js 逻辑，安全保存外部 API 秘钥（如企业私有 ERP 密钥）。</li>
    </ul>
  </section>
</ArticleLayout>
